---
name: pulsar
description: Enforces Pulsar vertical slice architecture rules when generating or modifying Laravel code. Activates when creating or changing controllers, requests, HTTP resources, use cases, operations, jobs, console commands, models, actions, queries, DTOs, value objects, enums, exceptions, events, listeners, notifications, mailables, policies, contracts, infrastructure adapters, or domains.
license: MIT
metadata:
    author: faran
    framework: laravel
    architecture: pulsar
    version: {{version}}
    pattern: vertical-slice
    strictness: mandatory
---

# Pulsar Architecture Skill

All Laravel code MUST follow Pulsar vertical slice architecture rules.
Violations are architectural errors.

---

## Mental Model

Three layers exist:

Service Layer: `app/Pulsar/Services/`
Domain Layer: `app/Pulsar/Domain/`
Infrastructure Layer: `app/Pulsar/Infrastructure/`

`app/Pulsar` is the architecture boundary. Code inside this directory follows Pulsar placement, dependency, and transaction rules.
A type belongs inside when it calls, is called by, or implements a Pulsar type. Framework-owned
bootstrap and configuration—migrations, factories, seeders, `bootstrap/app.php`, `config/`, and
`routes/`—stay in stock Laravel paths.

Stock Laravel directories may coexist with `app/Pulsar` indefinitely. Pulsar governs code inside
its boundary; incremental adoption may proceed route-by-route and module-by-module.

Services are delivery boundaries scoped to a consumer audience (Admin, Client, Internal).
A Service is NOT a microservice, bounded context, or deployment unit.
Domain is Laravel-aware business logic shared across services and independent of delivery concerns.
Infrastructure contains concrete outbound adapters that implement Domain Contracts.

---

## Installation & Wiring

Run `pulsar install` after adding Pulsar to a Laravel application. It generates and registers
`PulsarServiceProvider`, then re-establishes policy, event-listener, and Artisan command
discovery for `App\Pulsar`. Listener globs are native to event discovery; the installer expands
the command glob to concrete directories for `withCommands()`.

The operation is idempotent. `--dry-run` prints the diff without writing, every
`bootstrap/app.php` write has a `.pulsar.bak` backup, `--force` re-applies the provider without
duplicating wiring, and an unrecognized bootstrap shape fails closed with manual instructions.
The explicit wiring remains compatible with `event:cache` and `optimize`.

For opt-in executable dependency checks, copy
`vendor/faran/pulsar/presets/PulsarArchitectureTest.php` into `tests/Arch/`. The preset is
recommended, never required.

---

## File Structure

Service Layer:

```
app/Pulsar/Services/{Service}/
  Providers/
  Routes/api.php
  Routes/web.php        # generated with make:service --web
  Modules/{Module}/
    Controllers/
    Requests/
    Resources/
    UseCases/
    Operations/
    Jobs/
    Commands/
```

Domain Layer:

```
app/Pulsar/Domain/{Domain}/
  Contracts/
  Models/
  Actions/
  Queries/
  DTOs/
  ValueObjects/
  Policies/
  Events/
  Listeners/
  Notifications/
  Mail/
  Enums/
  Exceptions/
```

Infrastructure Layer:

```
app/Pulsar/Infrastructure/{Area}/
  {Adapter}.php
```

---

## Naming Conventions

Service Layer:

| Type       | Pattern                     | Example               |
| ---------- | --------------------------- | --------------------- |
| Service    | `{Audience}`                | `Admin`, `Client`     |
| Module     | `{Feature}` plural          | `Orders`, `Products`  |
| Controller | `{Resource}Controller`      | `OrderController`     |
| Request    | `{Verb}{Resource}Request`   | `CreateOrderRequest`  |
| UseCase    | `{Verb}{Resource}UseCase`   | `CreateOrderUseCase`  |
| Operation  | `{Verb}{Resource}Operation` | `PriceOrderOperation` |
| Job        | `{Verb}{Resource}Job`       | `ProcessOrderJob`     |
| Command    | `{Verb}{Resource}Command`   | `ReconcileLedgerCommand` |
| Resource   | `{Entity}Resource`          | `OrderResource`       |

Domain Layer:

| Type      | Pattern                   | Example                      |
| --------- | ------------------------- | ---------------------------- |
| Domain    | `{Capability}` singular   | `Order`, `Catalog`           |
| Model     | `{Entity}`                | `Order`                      |
| Action    | `{Verb}{Entity}Action`    | `CreateOrderAction`          |
| DTO       | `{Entity}Data`            | `OrderData`                  |
| Query     | `{Describe}{Result}Query` | `FindActiveOrdersQuery`      |
| Event     | `{Entity}{PastTenseVerb}` | `OrderCreated`               |
| Enum      | `{Entity}{Concept}`       | `OrderStatus`                |
| Exception | `{Descriptive}Exception`  | `InsufficientStockException` |
| Policy    | `{Entity}Policy`          | `OrderPolicy`                |
| Contract  | `{Capability}`            | `PaymentGateway`, `Clock`    |
| Listener  | `{Verb}{EventSubject}`     | `SendOrderReceipt`           |
| Notification | `{Subject}Notification` | `OrderReceiptNotification`   |
| Mailable  | `{Subject}Mail`            | `OrderReceiptMail`           |
| Value Object | `{DomainPrimitive}`     | `OrderNumber`                |

Infrastructure Layer:

| Type    | Pattern                     | Example                        |
| ------- | --------------------------- | ------------------------------ |
| Adapter | `{Vendor}{Capability}`      | `StripePaymentGateway`         |

---

## Layer Responsibilities

**Controller** — Extract validated data, call exactly one UseCase, and assemble the HTTP response. No business logic. Never call Queries, Operations, or Actions directly.

**Request** — Validate input structure only. No business rules. Authorization via `authorize()` is allowed.

**Resource** — Reusable field-level shaping of Models, DTOs, and Value Objects for HTTP. Response shaping never belongs in Domain.

**Job** — Queue entrypoint in an audience Service module. Re-establish context, authorize, and
call one UseCase. Carry IDs/DTOs/Value Objects, never Eloquent models. No transaction or
branching business logic; retries must be idempotent.

**Command** — CLI/scheduler entrypoint in an audience Service module. Validate, authorize, call
one UseCase, and return an exit code. No transaction or branching business logic.

**UseCase** — Orchestrate workflow. Own `DB::transaction()`. Coordinate Actions, Operations, Queries. Emit Events. Never accept Request object. Never call another UseCase. Only UseCases invoke Operations.

**Operation** — Reusable workflow fragment shared across UseCases. Compose Actions and Queries and allow conditional branching when needed. No nested Operations. No transactions. No event emission. Never called directly by Controllers.

**Action** — Atomic business operation. Validate business invariants. No other Action calls. No Query calls. No transactions. No Service layer dependencies.

**Query** — Read-only. No mutations. No Action calls. No nested Queries. No Service dependencies.

**Model** — Relationships, fillable, casts, accessors, scopes. No business logic beyond that.

**DTO** — `readonly` class, constructor promotion, static `from(array $data)`. No logic.

**Event** — Final readonly fact with a `VERSION` constant. Implements `ShouldDispatchAfterCommit` by default. Carries IDs, scalars, DTOs, or Value Objects, never Eloquent models across queued or durable boundaries. Emitted by UseCases only.

**Listener** — Domain reaction. A synchronous Listener performs a side effect/read-only check and
never calls a UseCase. A queued Listener uses `ShouldQueue` plus
`ShouldQueueAfterCommit`, may call one UseCase, and is idempotent with a reentrancy guard.

**Notification/Mailable** — Domain-owned outbound communication carrying DTO/VO data. No
workflow orchestration.

**Value Object** — Final readonly domain primitive. Validate in the constructor so invalid
instances cannot exist; provide a named constructor and value equality.

**Enum** — Backed enum only (`string` or `int`).

**Exception** — Extends `Exception`. Descriptive name communicating the business rule violated.

**Policy** — Without `--model`, generates only the `before()` hook. With `--model`, adds `view`, `create`, `update`, and `delete` methods typed to the domain model. Methods return `bool` and default deny. `before()` may return `true` for an admin bypass or `null` to defer.

**Contract** — Domain-owned port for a stable capability. Use capability names without
`Contract` or `Interface` suffixes (the generator strips a trailing one if you include it).
Signatures may reference passive Domain types but never call
workflow types or import Services/concrete Infrastructure.

**Adapter** — Infrastructure implementation of a Domain Contract. Translate vendor/framework
errors into Domain exceptions at this boundary. Never import Services, UseCases, Actions, or
Operations.

Generated policies assume the authenticatable is `App\Models\User`. If your application uses a different user model, update the generated import.

---

## Dependency Rules

Allowed:

```
Every Controller method -> exactly one UseCase
Command/Job -> one UseCase
Sync Listener -> Contract side effect, Notification, or Job
Queued Listener -> at most one UseCase
UseCase    -> Action, Operation, Query, Event (emit), Contract
Operation  -> Action, Query, Model, Contract
Action     -> Model, Contract
Query      -> Model
Infrastructure -> Domain Contract, DTO, Enum, Value Object, Domain Exception
```

Forbidden:

```
Controller -> Query, Operation, or Action directly
Command/Job -> Action, Operation, transaction, or multiple UseCases
Sync Listener -> UseCase
UseCase    -> UseCase
Action     -> Action
Action     -> Query
Query      -> Query or Action
Operation  -> Operation or UseCase
Domain     -> Service (any direction)
Service A  -> Service B
Domain/Service -> Concrete Infrastructure
Infrastructure -> Service, UseCase, Action, Operation, other domains' internals
Event emission outside UseCases
```

---

## Inbound Adapter Rule and Call Graphs

Every inbound adapter is thin. Every Controller method calls exactly one UseCase. Jobs and Commands
also validate, authorize, establish actor/tenant/correlation context, and call exactly one UseCase.
Controllers never call Queries directly. A read-only UseCase may call one or more Queries and need
not open a transaction. After the UseCase returns, a Controller may perform delivery-only response
assembly. It owns no transaction and contains no branching business logic. Commands, Jobs, and
Listeners have no Form Request, so their authorization touchpoint is explicit.

This rule applies to mutations, reads, and pages without Domain data. Extra UseCase scaffolding is
deliberate: Pulsar is agentic-first and favors one mechanically enforceable call graph over asking
an agent to classify endpoint complexity or choose an application entrypoint type.

```text
HTTP:      Request   → Controller → UseCase → {Actions, Operations, Queries, Events}
Artisan:   Console   → Command    → UseCase → ...
Queue:     Worker    → Job        → UseCase → ...
Scheduler: Schedule  → Command|Job → UseCase → ...
Event:     UseCase   → Event → Listener → {Contract side effect | Notification/Job | (queued) UseCase}
```

Machine entrypoints normally live in an `Internal` Service. Jobs never serialize Eloquent
models; carry explicit identifiers or immutable snapshots and re-establish context on the
worker. Queues are at-least-once, so Jobs and queued Listeners must be idempotent.

---

## Contracts and Infrastructure

Contracts live at `app/Pulsar/Domain/{Domain}/Contracts/{Name}.php`. The consumer owns the port:
Billing defines `PaymentGateway` when Billing needs payment behavior; a concrete adapter lives at
`app/Pulsar/Infrastructure/Payments/StripePaymentGateway.php`.

Distinguish passive types from behavioral calls:

- A Contract signature may passively reference DTOs, Enums, Value Objects, Models, Events, other
  Contracts, and Laravel contracts.
- A Contract never behaviorally invokes a UseCase, Action, or Operation.
- A Contract never imports Services or concrete Infrastructure.

What does NOT belong in `Contracts/`: DTOs, Value Objects, Enums, traits, helper classes,
concrete implementations, framework aliases, or interface-per-class wrappers around pure Domain
logic. A capability earns a Contract when it has a real alternate implementation, test fake, or
varying strategy.

Bind implementations in `PulsarServiceProvider::register()`:

```php
$this->app->bind(
    \App\Pulsar\Domain\Billing\Contracts\PaymentGateway::class,
    \App\Pulsar\Infrastructure\Payments\StripePaymentGateway::class,
);
```

Stateless adapters may use `singleton()`. Request- or tenant-state adapters use `scoped()` and
resolve context through closures, never constructor capture. Retryable side effects must be
idempotent.

### Event-name Contract via per-domain backed Enum

```php
// Domain/Shared/Contracts/DomainEventName.php
interface DomainEventName
{
    public function eventName(): string;
}

// Domain/Orders/Enums/OrderEventName.php
enum OrderEventName: string implements DomainEventName
{
    case Placed = 'orders.order_placed'; // Persisted/protocol identifier: immutable.
    case Cancelled = 'orders.order_cancelled';

    public function eventName(): string
    {
        return $this->value;
    }

    public function label(): string
    {
        return match ($this) {
            self::Placed => 'Order placed',
            self::Cancelled => 'Order cancelled',
        };
    }
}
```

Do not accept arbitrary `BackedEnum` and do not create a cross-domain god enum. Backed values are
stable machine identifiers; human labels belong in `label()` and may change. Event payload schema
versioning belongs on the Event class as `public const int VERSION = 1`.

### Magic-string Policy

| Category | Recommendation | Why |
|----------|----------------|-----|
| Persisted business states | Per-domain string-backed Enum | Closed, domain-owned, type-safe set |
| Persisted event names | Per-domain backed Enum implementing an event-name Contract | Stable machine identifier without a god enum |
| Event categories | Enum | Closed set |
| Authorization abilities | Per-domain string-backed Enum, passed to Gate as `->value` | No free-floating ability strings |
| Queue connections and queue names | Config keys, referenced by constants | Operational and environment-varying |
| Event listener names | Class names | Laravel registration is class-based |
| Artisan command signatures | String on the command class | Framework-required, single source |
| Configuration keys | Config files; typed accessor for hot paths | Laravel convention |
| Route names | Namespaced string such as `{service}.*` | Framework string API |
| External protocol identifiers | Immutable constant or backed Enum | Protocol stability |

Closed sets use Enums; open/environment-varying sets use config. Convert Enums at framework
boundaries with `->value`, never use human labels as machine identifiers, and never casually
rename persisted/protocol values.

---

## Transaction Ownership

Only UseCases call `DB::transaction()`.
Actions, Operations, and Queries never start transactions.

---

## Data Flow Rules

Constructor = dependencies only (injected services).
`execute()` = runtime data (DTOs, IDs, arrays).
Request objects never cross Controller boundary — convert to DTO or `$request->validated()` first.
Actions, Queries, and UseCases return delivery-neutral domain types (models, collections, DTOs,
Value Objects, primitives, arrays, or void). Never return Resources, Inertia prop wrappers,
redirects, or HTTP responses.

---

## Generated Method Conventions

| Type | Public workflow method |
|------|------------------------|
| Action | `execute` |
| Operation | `execute` |
| Query | `execute` |
| UseCase | `execute` |

---

## Cross-Domain Coordination

When a workflow spans multiple domains (e.g., Order + Inventory + Payment), the UseCase coordinates. Actions stay within their own domain. Never have an Action call an Action from another domain.

---

## Event Side Effects

Events are created and dispatched by UseCases only. Actions and Operations never emit them.
Synchronous Listeners handle side effects/read-only checks without calling a UseCase or assuming
durability. Queued Listeners implement `ShouldQueue` and `ShouldQueueAfterCommit`, may call one
UseCase, and must be idempotent with a reentrancy/loop guard. Never call mail, notification, or
external services directly inside a UseCase or Action.

Use this four-tier rule:

| Tier | Mechanism | Guarantee | Use when |
|------|-----------|-----------|----------|
| 1. Sync in-process | Plain `event()` in the current request | Runs immediately and fires before commit if called inside a transaction | Pure in-memory reactions only; never committed-fact side effects |
| 2. After-commit | Event implements `ShouldDispatchAfterCommit` | Discarded on rollback and dispatched after commit; an in-memory event can still be lost if the process crashes between commit and dispatch | Default for domain events |
| 3. Queued after-commit | Queued listener uses `ShouldQueue` + `ShouldQueueAfterCommit`, or a Job uses `afterCommit()` | Runs on a worker after commit with at-least-once delivery; handlers must be idempotent, and enqueueing is still not atomic with the database write | External calls, expensive side effects, cross-aggregate work |
| 4. Outbox/inbox | Write an outbox row in the same transaction and publish it with a relay | Durable across crashes; consumers use inbox/idempotency for effectively-once effects | Integration events that require guaranteed external delivery |

Tier 2 prevents rollback leaks and read-before-commit races, but it does not close the
crash-between-commit-and-dispatch durability gap. Use tier 4 when that guarantee matters.

Generated Events use tier 2:

```php
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

final readonly class OrderCreated implements ShouldDispatchAfterCommit
{
    public const int VERSION = 1;

    public function __construct(public int $orderId) {}
}
```

---

## Routing

Routes live inside the Service, not in Laravel's default `routes/` directory.

```
app/Pulsar/Services/Admin/Routes/api.php
app/Pulsar/Services/Admin/Routes/web.php
```

`make:service Admin` generates the prefixed API surface. `make:service Admin --web` additionally
generates an unprefixed `web.php` loaded with Laravel's `web` middleware. Put any desired browser
URL or route-name prefix inside `web.php`, and register the Service provider class printed by the
command in `bootstrap/providers.php`.

### Inertia Delivery

Pulsar does not install Inertia. The application owns the root view, client adapter, and
`HandleInertiaRequests` middleware. The Controller owns top-level response assembly; a Service
Resource owns reusable field-level shaping. Cross-page shared props belong in
`HandleInertiaRequests`; Inertia lazy/deferred/optional wrappers and partial-reload prop names
belong in the Controller. Queries and UseCases never return Resources, Inertia wrappers,
redirects, or response objects.

```php
Route::get('/orders', [OrderController::class, 'index'])->name('orders.index');
Route::post('/orders', [OrderController::class, 'store'])->name('orders.store');

class OrderController extends Controller
{
    public function index(ListOrdersUseCase $useCase): \Inertia\Response
    {
        return Inertia::render('Orders/Index', [
            'orders' => OrderResource::collection($useCase->execute()),
        ]);
    }

    public function store(PlaceOrderRequest $request, PlaceOrderUseCase $useCase): RedirectResponse
    {
        $useCase->execute(OrderData::from($request->validated()));

        return to_route('orders.index');
    }
}
```

Invalid Inertia form submissions use Laravel's normal redirect-and-flash validation flow. Do not
override `failedValidation()` or return a manual `422` response for a browser page route. Error
bags, old input, and redirect status handling remain Laravel/Inertia delivery behavior.

---

## End-to-End Example

This single example demonstrates the full flow: Controller → UseCase → Action, with correct namespaces, data flow, and transaction ownership.

```php
// app/Pulsar/Services/Admin/Modules/Orders/Controllers/OrderController.php
namespace App\Pulsar\Services\Admin\Modules\Orders\Controllers;

use App\Pulsar\Domain\Order\DTOs\OrderData;
use App\Pulsar\Services\Admin\Modules\Orders\Requests\CreateOrderRequest;
use App\Pulsar\Services\Admin\Modules\Orders\UseCases\CreateOrderUseCase;

class OrderController extends Controller
{
    public function store(CreateOrderRequest $request, CreateOrderUseCase $useCase)
    {
        $order = $useCase->execute(OrderData::from($request->validated()));
        return response()->json($order, 201);
    }
}
```

```php
// app/Pulsar/Services/Admin/Modules/Orders/UseCases/CreateOrderUseCase.php
namespace App\Pulsar\Services\Admin\Modules\Orders\UseCases;

use App\Pulsar\Domain\Order\Actions\CreateOrderAction;
use App\Pulsar\Domain\Inventory\Actions\ReserveStockAction;
use App\Pulsar\Domain\Order\DTOs\OrderData;
use App\Pulsar\Domain\Order\Events\OrderCreated;
use Illuminate\Support\Facades\DB;

class CreateOrderUseCase
{
    public function __construct(
        private CreateOrderAction $createOrder,
        private ReserveStockAction $reserveStock,
    ) {}

    public function execute(OrderData $data): Order
    {
        $order = DB::transaction(function () use ($data) {
            $order = $this->createOrder->execute($data);
            $this->reserveStock->execute($order->items);

            return $order;
        });

        event(new OrderCreated($order->id));

        return $order;
    }
}
```

```php
// app/Pulsar/Domain/Order/Actions/CreateOrderAction.php
namespace App\Pulsar\Domain\Order\Actions;

use App\Pulsar\Domain\Order\DTOs\OrderData;
use App\Pulsar\Domain\Order\Models\Order;

class CreateOrderAction
{
    public function execute(OrderData $data): Order
    {
        return Order::create([
            'customer_id' => $data->customerId,
            'status'      => OrderStatus::Pending,
        ]);
    }
}
```

This example also demonstrates cross-domain coordination: the UseCase imports from both `Order` and `Inventory` domains. The Actions themselves have no knowledge of each other.

---

## Forbidden Patterns

- Fat Controllers containing business logic
- Controllers calling Queries directly
- Controllers calling Operations directly
- Actions calling other Actions
- Actions calling Queries (pass the data in instead)
- Operations emitting domain events
- Events emitted outside UseCases; Actions/Operations never emit
- Transactions inside Actions or Operations
- UseCases calling other UseCases
- Request objects leaking past Controller
- Business logic in Models beyond accessors/scopes

If you feel tempted to do any of the above, the architecture is being violated.

---

## Commands Reference

Use Pulsar CLI to generate files to ensure correct namespace and location.

| Command | Arguments and options |
|---------|-----------------------|
| `install` | `[--dry-run] [--force]` |
| `make:service` | `{name} [--web]` |
| `make:controller` | `{name} {module} {service} [--resource]` |
| `make:request` | `{name} {module} {service}` |
| `make:resource` | `{name} {module} {service} [--collection]` |
| `make:use-case` | `{name} {module} {service}` |
| `make:operation` | `{name} {module} {service}` |
| `make:job` | `{name} {module} {service}` |
| `make:command` | `{name} {module} {service} [--signature={signature}]` |
| `make:domain` | `{name}` |
| `make:contract` | `{name} {domain}` |
| `make:model` | `{name} {domain}` |
| `make:action` | `{name} {domain}` |
| `make:dto` | `{name} {domain}` |
| `make:policy` | `{name} {domain} [--model={model}]` |
| `make:event` | `{name} {domain}` |
| `make:listener` | `{name} {domain} [--event={event}] [--queued]` |
| `make:notification` | `{name} {domain}` |
| `make:mailable` | `{name} {domain}` |
| `make:enum` | `{name} {domain}` |
| `make:value-object` | `{name} {domain}` |
| `make:exception` | `{name} {domain}` |
| `make:query` | `{name} {domain}` |
| `make:adapter` | `{name} {area} [--contract={FQCN\|name}] [--domain={domain}]` |
| `publish:context` | `[--force] [--path={path}]` |
| `publish:skill` | `[--force] [--path={path}]` |
| `ping` | — |

---

## Uncertainty Protocol

If unsure about placement, layer type, or domain ownership:
STOP and ask the user.
Never guess.
